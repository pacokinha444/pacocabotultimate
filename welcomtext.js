const welcome = (number, groupname) => {
    return `eai @${number}. bem vindo ao gp ${groupname}`
}
exports.welcome = welcome

const bye = (number) => {
    return `flw @${number}. volta mais n tb`
}
exports.bye = bye
